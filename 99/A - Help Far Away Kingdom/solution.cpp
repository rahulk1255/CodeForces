#include <bits/stdc++.h>using namespace std; string print_updated_string(string s) {    for (int i = s.size() - 1; i >= 0; i--) {        if (s[i] < '9') {            s[i] = char((s[i] - '0') + 1 + '0');            return s;        }        s[i] = '0';    }    s.insert(s.begin(), '1');    return s;} int main() {    string s;    cin >> s;     for (int i = 0; i < s.size(); i++) {        if (s[i] == '.') {            if (s[i - 1] == '9') {                cout << "GOTO Vasilisa.";            } else {                if (s[i + 1] >= '5') {                    cout << print_updated_string(s.substr(0, i));                } else {                    cout << s.substr(0, i);                }            }            return 0;        }    }}